#include "register.hpp"
#include <map>
class stack{
private:
	int stack_end;
	int stack_pointer;
	std::map<std::string, int> memory_map;
public:
	int mem_to_reg(std::string name. std::string )


	friend class registers;
};
